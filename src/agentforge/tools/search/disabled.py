"""Disabled_Search_Provider — the keyless default web-search provider (stub).

Will implement ``Search_Provider`` with ``available = False`` and a ``search`` that never
performs a network request, so the platform runs with no search credential (Req 5.4).
Implemented in a later task (see task 6.1); this module is a scaffolding placeholder.
"""

from __future__ import annotations
