"""Tool-call parsing and the deterministic fallback Selection_Strategy (stub).

Will hold prompt construction that serializes available Tool_Specs, ``parse_decision``
that extracts a structured decision from LLM text, and the ``Selection_Strategy`` seam
with a deterministic fallback that is a pure function of run state (Req 3.1, 3.5, 1.8).
Implemented in a later task (see task 4.1); this module is a scaffolding placeholder.
"""

from __future__ import annotations
