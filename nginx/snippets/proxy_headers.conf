# Shared proxy headers (deployment Req 6.5). Forward the standard X-Forwarded-*
# context so the backend can reconstruct the original request; request/response
# bodies are never altered in transit — the HTTP/SSE contract is byte-preserved.
proxy_http_version 1.1;
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
proxy_set_header X-Forwarded-Host  $host;
proxy_set_header X-Forwarded-Port  $server_port;
